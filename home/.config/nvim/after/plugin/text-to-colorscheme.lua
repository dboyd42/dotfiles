--[[
  1. Don't want to pay for ChatGPT-4
  2. Don't want to waste time to configuring reverting colorscheme
  Therefore, will deal with later...
]]
-- local map = vim.keymap.set
--
-- require('text-to-colorscheme').setup({
--   ai = {
--     -- gpt_model = "gpt-4", -- Default
--     gpt_model = "gpt-3", -- Default
--     openai_api_key = os.getenv("OPENAI_API_KEY"),
--   }, -- end ai
-- })
--
-- map('n', '<leader>cc', '<cmd>colorscheme text-to-colorscheme<CR>')
